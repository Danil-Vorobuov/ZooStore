<Window x:Class="Zoomagazin.Poctavchiki"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:Zoomagazin"
        mc:Ignorable="d"
           Title="Users" Height="500" Width="850" Background="Transparent" WindowStartupLocation="CenterScreen" WindowStyle="None" 
    AllowsTransparency="True">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Border CornerRadius="20" Background="#eff2f7">
        <Grid Margin="10" >

            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Icon:PackIconMaterial Margin="0 0 0 0" Kind="Close" Grid.Column="1" Panel.ZIndex="1" Mouse.MouseUp="Icon_MouseUp" VerticalAlignment="Top" Style="{StaticResource iconClose}"/>

            <TextBlock Text="Поставки" Grid.Row="1" FontSize="32" FontWeight="SemiBold" Margin="0 20 0 0"
           HorizontalAlignment="Left" Foreground="#3AB19B"/>

            

            <DataGrid 
                Style="{DynamicResource DataGridStyle1}" 
                CellStyle="{DynamicResource DataGridCellStyle1}" 
                ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" 
                RowStyle="{DynamicResource DataGridRowStyle1}" 
                Grid.Row="5" 
                x:Name="PoctavDataGrid">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Поставщик" IsReadOnly="True" Width="Auto" Binding="{Binding Suppliers.Name}" />
                    <DataGridTextColumn Header="Куда" IsReadOnly="True" Width="180" Binding="{Binding SalesPoints.Address}"/>
                    <DataGridTextColumn Header="Дата" IsReadOnly="True" Width="180">
                        <DataGridTextColumn.Binding>
                            <Binding Path="date">
                                <Binding.StringFormat>dd.MM.yyyy</Binding.StringFormat>
                            </Binding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="Документ" IsReadOnly="True" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Открыть" Click="OpenFile_Click" Tag="{Binding Exel}" Background="#3AB19B"
                                IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                                Style="{StaticResource MaterialDesignRaisedDarkButton}"
                                BorderThickness="0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Операции" IsReadOnly="True" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource gridRemoveButton}" Click="Deletez">
                                        <Icon:PackIconMaterial Kind="DeleteOutline" Style="{StaticResource gridButtonIcon}"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

        </Grid>
    </Border>
</Window>

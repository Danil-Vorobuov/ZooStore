<Window x:Class="Zoomagazin.Polzovatel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:Zoomagazin"
        mc:Ignorable="d"
        Title="Users" Height="500" Width="800" Background="Transparent"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Border CornerRadius="20" Background="#eff2f7">
        <Grid Margin="10">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Icon:PackIconMaterial Margin="0 0 0 0" Kind="Close" Grid.Column="1" Panel.ZIndex="1" 
                                    Mouse.MouseUp="Icon_MouseUp" VerticalAlignment="Top" 
                                    Style="{StaticResource iconClose}"/>

            <TextBlock Text="Пользователи" Grid.Row="1" FontSize="32" FontWeight="SemiBold" Margin="0 20 0 0"
                       HorizontalAlignment="Left" Foreground="#3AB19B"/>

            <Button Style="{StaticResource addButton}" Width="170" Click="UserAdd" Grid.Row="1">
                <StackPanel Orientation="Horizontal">
                    <Icon:PackIconMaterial Kind="Plus" VerticalAlignment="Center" Width="11" Height="11" Margin="0 1 8 0"/>
                    <TextBlock Text="Добавить пользователя"/>
                </StackPanel>
            </Button>

            <Separator Height="1" Grid.Row="3" Background="#dae2ea" Margin="0 10 0 0"/>

            <TextBlock Text="Поиск пользователей" Grid.Row="4" FontSize="13" FontWeight="SemiBold" Margin="0 0 215 0"
                       Foreground="#121518" HorizontalAlignment="Right" VerticalAlignment="Center"/>

            <Grid Width="200" HorizontalAlignment="Right" Grid.Row="4">
                <TextBlock Margin="15 0" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False"
                           Panel.ZIndex="1" Text="Введите название товара ..." 
                           Visibility="{Binding ElementName=txtFilter, Path=Text, Converter={StaticResource BoolToVis}}" />
                <TextBox x:Name="txtFilter" Style="{StaticResource textboxFilter}" TextChanged="txtFilter_TextChanged"/>
            </Grid>

            <DataGrid 
                Style="{DynamicResource DataGridStyle1}" 
                CellStyle="{DynamicResource DataGridCellStyle1}" 
                ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" 
                RowStyle="{DynamicResource DataGridRowStyle1}" 
                Grid.Row="5" 
                x:Name="UsersDataGrid"
                AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Сотрудник" IsReadOnly="True" Width="Auto" Binding="{Binding FullName}" />
                    <DataGridTextColumn Header="Username" IsReadOnly="True" Width="180" Binding="{Binding Username}"/>
                    <DataGridTextColumn Header="Password" IsReadOnly="True" Width="180" Binding="{Binding Password}"/>
                    <DataGridTextColumn Header="Должность" IsReadOnly="True" Width="180" Binding="{Binding Position}"/>
                    <DataGridTemplateColumn Header="Операции" IsReadOnly="True" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource gridEditButton}" Click="EditUserButton_Click">
                                        <Icon:PackIconMaterial Kind="PencilOutline" Style="{StaticResource gridButtonIcon}"/>
                                    </Button>
                                    <Button Style="{StaticResource gridRemoveButton}" Click="DeleteUserButton_Click">
                                        <Icon:PackIconMaterial Kind="DeleteOutline" Style="{StaticResource gridButtonIcon}"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

        </Grid>
    </Border>
</Window>

<Window x:Class="Zoomagazin.Window3"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:Zoomagazin"
        mc:Ignorable="d"
       Title="MessageBox" Height="250" Width="500" AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterScreen" WindowStyle="None">
    <Window.Resources>
        <Style TargetType="Border" x:Key="MessageBoxBorderStyle">
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="Background" Value="#eff2f7"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Style="{StaticResource MessageBoxBorderStyle}">
            <Grid>
                <StackPanel Orientation="Vertical">
                    <StackPanel Margin="0 0 0 0" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
                        <TextBlock Text="Редактирование профиля" Foreground="#3AB19B" FontSize="20" />
                        <Icon:PackIconMaterial Kind="Close" Grid.Column="1" Panel.ZIndex="1" Margin="220 10 0 0" Width="15" Height="15" Mouse.MouseUp="Icon_MouseUp" Style="{StaticResource iconClose}"/>

                    </StackPanel>
                    <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20 20 0 5" Orientation="Horizontal">
                        <Ellipse Width="150" Height="150">
                            <Ellipse.Fill>
                                <ImageBrush x:Name="UserImage"/>
                            </Ellipse.Fill>
                        </Ellipse>
                        <StackPanel Width="330">
                            <TextBox x:Name="UserName"
                                  VerticalAlignment="Top" Margin="70 0 70 15"
                                  materialDesign:HintAssist.HelperText="Имя пользователя"
                                  Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                            <TextBox x:Name="Password"
                                  VerticalAlignment="Top" Margin="70 10 70 15"
                                  materialDesign:HintAssist.HelperText="Пароль"
                                  TextWrapping="Wrap"
                                  Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        </StackPanel>
                    </StackPanel>

                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" >

                            <Button
                            Margin="0 5 5 0"
                            Height="40"
                            Width="150"
                            Background="#3AB19B" 
                            materialDesign:ButtonAssist.CornerRadius="20"
                            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                            Style="{StaticResource MaterialDesignRaisedDarkButton}"
                            BorderThickness="0"
                            ToolTip="Загрузит фотографию пользователя"
                            Click="UploadPhoto_Click">
                                <TextBlock Text="Загрузить фото"/>
                            </Button>
                            <Button
                            Margin="5 5 5 0"
                            Height="40"
                            Width="150"
                            Background="#3AB19B" 
                            materialDesign:ButtonAssist.CornerRadius="20"
                            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                            Style="{StaticResource MaterialDesignRaisedDarkButton}"
                            BorderThickness="0"
                            ToolTip="Сохранить изменения"
                            Click="SaveChanges_Click">
                                <TextBlock Text="Сохранить"/>
                            </Button>
                        </StackPanel>
                    </Grid>

                </StackPanel>
                <Grid x:Name="DimBackground" Visibility="Collapsed" Background="Black" Opacity="0.5" MouseLeftButtonDown="AnotherWindow_Closed"/>
            </Grid>
        </Border>
    </Grid>
</Window>

<Window x:Class="Zoomagazin.MessegeBoxClosed"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:Zoomagazin"
        mc:Ignorable="d"
           Title="MessageBox" Height="150" Width="300" AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterScreen" WindowStyle="None">
    <Window.Resources>
        <Style TargetType="Border" x:Key="MessageBoxBorderStyle">
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="Background" Value="#eff2f7"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Style="{StaticResource MessageBoxBorderStyle}">
            <StackPanel Orientation="Vertical">
                <StackPanel Margin="0 30 0 50" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Вы действительно хотите выйти?" Foreground="#3AB19B" FontSize="15" />
                </StackPanel>
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" >
                        <Button
                        Margin="0 0 0 0"
                        Click="YesClos"
                        Height="40"
                        Width="100"
                        Background="#3AB19B" 
                        materialDesign:ButtonAssist.CornerRadius="20"
                        IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignRaisedDarkButton}"
                        BorderThickness="0"
                        ToolTip="MaterialDesignRaisedDarkButton with Round Corners">
                            <TextBlock Text="Да"/>
                        </Button>
                        <Button
                        Margin="15 0 10 0"
                        Click="NoClos"
                        Height="40"
                        Width="100"
                        Background="#3AB19B" 
                        materialDesign:ButtonAssist.CornerRadius="20"
                        IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignRaisedDarkButton}"
                        BorderThickness="0"
                        ToolTip="MaterialDesignRaisedDarkButton with Round Corners">
                            <TextBlock Text="Нет"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>
